# TOASK

## Dúvidas Gerais

* A `Main` do Kind2 deveria virar um `run` no kdl?

## Dúvidas relacionadas a arrancar código inútil/repetido

* As coisas que printam tipos primitivos (`Type` e `U32`) podem só ser purgadas | reduzidos a `@x(x)`?
* Kdl não é tipada, mas talvez faça sentido ter construtores pros tipos?
  * Ex.: Funções/Ctrs que recebam um Type como arg, que não seja pra tipar outros args?

## TODO

* FLATTENER
* Descobrir como obter as funções na ordem de entrada, e não na ordem do mapa
* Variáveis usadas múltiplas vezes precisam ser `dup`adas
* Se o name de um `let` é usado múltiplas vezes, tem que fazer `dup`
  * Descobrir como fazer pra mais de 2 usos
* Como definir o Initial State de uma Função?
  * Proposta do Kelvin: Para toda função `foo`, existir um `foo.state` - de aridade 0 - declarando o initial state
